import { FaShoppingCart } from "react-icons/fa";

const CartWidget = () => {
    return (
        <div>
            <img  src={FaShoppingCart} alt="cart-widget" />
        </div>
    )
}

export default CartWidget;